import { GendatesearchPipe } from './gendatesearch.pipe';

describe('GendatesearchPipe', () => {
  it('create an instance', () => {
    const pipe = new GendatesearchPipe();
    expect(pipe).toBeTruthy();
  });
});
